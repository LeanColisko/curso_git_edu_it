comandos git vistos en clase 1:

config
init
add
status
commit


log 
log --oneline
git show 'con commit id'
git blame

Para eliminar cambios que no quiero añadir
git restore


Elimina y lleva el archivo a la zona de staging
git rm 'archivo'


Cuando el estado en staging area
git diff 'archivo.txt'

Agrgamos archivo para ignorar archivos del trackeo
touch .gitignore

git remote add origin git@github.com:LeanColisko/curso_git_edu_it.git

Una vez que agregamos el repositorio remoto, lo vemos en nuestra configuracion:
git config --list


git push -u origin master



COMANDOS CLASE 2:

git clone

git branch

git switch

git merge


